package com.interface_adapter.temp;

public class TempState {

    public TempState(TempState copy) {}

    // Because of the previous copy constructor, the default constructor must be explicit.
    public TempState() {}
}

package com.interface_adapter.mainMap;

public class MainMapState {

    public MainMapState(MainMapState copy) {}

    // Because of the previous copy constructor, the default constructor must be explicit.
    public MainMapState() {}
}
